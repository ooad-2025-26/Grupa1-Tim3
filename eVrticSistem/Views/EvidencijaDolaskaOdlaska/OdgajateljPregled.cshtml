@model EVrtic.Controllers.OdgajateljEvidencijaPregledViewModel

@{
    ViewData["Title"] = "Dolazak / Odlazak";
}

@functions {
    string FormatStatus(EVrtic.Models.StatusEvidencije status)
    {
        return status.ToString().Replace("_", " ").ToLower();
    }
}

<div class="educator-attendance-page">
    <div class="educator-attendance-card">

        <aside class="educator-sidebar">
            <h1 class="educator-logo">eVrtić</h1>

            <nav class="educator-menu">
    <a class="educator-link" asp-controller="Home" asp-action="OdgajateljHome">
        <span>🏠</span>
        Početna
    </a>

    <a class="educator-link" asp-controller="DnevniIzvjestaj" asp-action="EvidencijaAktivnosti">
        <span>📝</span>
        Aktivnosti
    </a>

    <a class="educator-link active" asp-controller="EvidencijaDolaskaOdlaska" asp-action="OdgajateljPregled">
        <span>☑️</span>
        Dolazak/Odlazak
    </a>

    <a class="educator-link" asp-controller="Obavijest" asp-action="OdgajateljObavijesti">
        <span>🔔</span>
        Obavijesti
    </a>

    <a class="educator-link" asp-controller="SazetakAktivnosti" asp-action="OdgajateljPregled">
        <span>📊</span>
        Sažeci
    </a>

    <a class="educator-link" asp-controller="Korisnik" asp-action="Profil">
        <span>👤</span>
        Profil
    </a>

    
</nav>

            <div class="sidebar-decoration">
                <div class="cloud cloud-left"></div>
                <div class="cloud cloud-right"></div>

                <div class="ground-illustration">
                    <div class="tree tree-left"><div class="tree-top"></div><div class="tree-trunk"></div></div>
                    <div class="tree tree-right"><div class="tree-top"></div><div class="tree-trunk"></div></div>

                    <div class="house-illustration">
                        <div class="house-roof"></div>
                        <div class="house-body">
                            <div class="house-window"></div>
                            <div class="house-door"></div>
                            <div class="house-window"></div>
                        </div>
                    </div>
                </div>
            </div>
        </aside>

        <main class="educator-main">
            <div class="page-top">
                <div class="title-row">
                    <a asp-controller="Home" asp-action="OdgajateljHome" class="back-link">←</a>
                    <h1>Dolazak / Odlazak</h1>
                </div>
                <div class="qr-corner">▦</div>
            </div>

            <section class="intro-card">
                <div class="intro-icon">▦</div>
                <div>
                    <h2>Pregled evidencije dolaska i odlaska</h2>
                    <p>Pregledajte evidentirane dolaske i odlaske za djecu iz vaših grupa.</p>
                </div>
            </section>

            @if (TempData["Poruka"] != null)
            {
                <div class="qr-flash">@TempData["Poruka"]</div>
            }

            <section class="qr-generate-card">
                <div class="qr-generate-info">
                    <h2>Dnevni QR kod</h2>
                    <p>Odaberite tip evidencije, zatim generišite QR kod za današnji dan. Roditelji ga skeniraju prilikom dolaska ili odlaska djeteta.</p>

                    <form method="post" asp-action="OdgajateljGenerisiQR" id="generisiForm">
                        @Html.AntiForgeryToken()
                        <input type="hidden" name="tip" id="odabraniTipInput" value="@Model.OdabraniTip" />

                        <div class="qr-type-toggle">
                            <button type="button" class="qr-type-btn @(Model.OdabraniTip == "DOLAZAK" ? "active" : "")" data-tip="DOLAZAK">↓ Dolazak</button>
                            <button type="button" class="qr-type-btn @(Model.OdabraniTip == "ODLAZAK" ? "active" : "")" data-tip="ODLAZAK">↑ Odlazak</button>
                        </div>

                        <button type="submit" class="qr-generate-btn">▦ Generiši QR kod</button>
                    </form>

                    @if (Model.AktivniQRKod != null)
                    {
                        <p class="qr-valid-text">QR kod važi do: @Model.AktivniQRKod.VrijemeIsteka.ToString("dd.MM.yyyy. HH:mm")</p>
                    }
                </div>

                <div class="qr-generate-preview">
                    @if (Model.AktivniQRKod != null)
                    {
                        <div id="qrcode" class="qr-box" data-token="@Model.AktivniQRKod.VrijednostKoda"></div>
                        <p class="qr-type-label" id="qrTypeLabel"></p>
                    }
                    else
                    {
                        <div class="qr-box qr-box-empty">▦</div>
                        <p class="qr-empty-label">Još nije generisan QR kod za danas.</p>
                    }
                </div>
            </section>

            <form class="attendance-filter" method="get" asp-action="OdgajateljPregled">
                <div class="field-block">
                    <label>Datum</label>
                    <div class="date-box">
                        <span>@Model.Datum.ToString("dd.MM.yyyy.")</span>
                        <span class="calendar-symbol">▣</span>
                        <input type="date" name="datum" value="@Model.Datum.ToString("yyyy-MM-dd")" />
                    </div>
                </div>

                <div class="field-block">
                    <label>Grupa</label>
                    <select name="grupaId">
                        <option value="">Sve grupe</option>
                        @foreach (var grupa in Model.Grupe)
                        {
                            <option value="@grupa.Id" selected="@(grupa.Id == Model.OdabranaGrupaId ? "selected" : null)">
                                @grupa.ImeGrupe
                            </option>
                        }
                    </select>
                </div>

                <div class="field-block">
                    <label>Dijete</label>
                    <select name="dijeteId">
                        <option value="">Sva djeca</option>
                        @foreach (var dijete in Model.Djeca)
                        {
                            <option value="@dijete.Id" selected="@(dijete.Id == Model.OdabranoDijeteId ? "selected" : null)">
                                @dijete.ImePrezime
                            </option>
                        }
                    </select>
                </div>

                <button type="submit">Prikaži</button>
            </form>

            <section class="stats-grid">
                <div class="stat-card">
                    <span class="stat-icon green">↓</span>
                    <h3>Dolasci</h3>
                    <strong>@Model.BrojDolazaka</strong>
                </div>

                <div class="stat-card">
                    <span class="stat-icon purple">↑</span>
                    <h3>Odlasci</h3>
                    <strong>@Model.BrojOdlazaka</strong>
                </div>
            </section>

            <section class="records-card">
                <div class="records-heading">
                    <h2>Evidencije za @Model.Datum.ToString("dd.MM.yyyy.")</h2>
                    <span>@Model.Evidencije.Count zapisa</span>
                </div>

                @if (!Model.Evidencije.Any())
                {
                    <div class="empty-state">
                        <div>▦</div>
                        <h3>Nema evidentiranih dolazaka ili odlazaka.</h3>
                        <p>Kada roditelj unese kod djeteta nakon skeniranja QR koda, zapis će se prikazati ovdje.</p>
                    </div>
                }
                else
                {
                    <div class="records-list">
                        @foreach (var evidencija in Model.Evidencije)
                        {
                            bool dolazak = evidencija.TipDogadjaja == EVrtic.Models.TipDogadjaja.DOLAZAK;
                            bool odbijeno = evidencija.StatusEvidencije == EVrtic.Models.StatusEvidencije.ODBIJENO;

                            <article class="record-item @(odbijeno ? "rejected" : "")">
                                <div class="record-icon @(dolazak ? "arrival" : "departure")">
                                    @(dolazak ? "↓" : "↑")
                                </div>

                                <div class="record-main">
                                    <h3>@(evidencija.Dijete?.ImePrezime ?? "Nepoznato dijete")</h3>
                                    <p>
                                        @(evidencija.Dijete?.Grupa?.ImeGrupe ?? "Bez grupe")
                                        · Kod: @evidencija.UneseniKodDjeteta
                                    </p>
                                </div>

                                <div class="record-meta">
                                    <strong>@evidencija.TipDogadjaja</strong>
                                    <span>@evidencija.VrijemeDogadjaja.ToString("HH:mm")</span>
                                </div>

                                <div class="record-status @(odbijeno ? "bad" : "good")">
                                    @FormatStatus(evidencija.StatusEvidencije)
                                </div>
                            </article>
                        }
                    </div>
                }
            </section>

<nav class="educator-bottom-nav">
    <a class="bottom-link" asp-controller="Home" asp-action="OdgajateljHome">
        <span>🏠</span> Početna
    </a>

    <a class="bottom-link" asp-controller="DnevniIzvjestaj" asp-action="EvidencijaAktivnosti">
        <span>📝</span> Aktivnosti
    </a>

    <a class="bottom-link active" asp-controller="EvidencijaDolaskaOdlaska" asp-action="OdgajateljPregled">
        <span>☑️</span> Dolazak/Odlazak
    </a>

    <a class="bottom-link" asp-controller="SazetakAktivnosti" asp-action="OdgajateljPregled">
        <span>📊</span> Sažeci
    </a>

    <a class="bottom-link" asp-controller="Obavijest" asp-action="OdgajateljObavijesti">
        <span>🔔</span> Obavijesti
    </a>
</nav>
        </main>
    </div>
</div>

<style>
    body { background: #eef3ff; }

    .educator-attendance-page {
        min-height: 100vh;
        padding: 22px 0;
        font-family: Arial, sans-serif;
        color: #111827;
    }

    .educator-attendance-card {
        width: 100%;
        max-width: 1250px;
        min-height: 760px;
        margin: 0 auto;
        background: #f7f8ff;
        border-radius: 22px;
        box-shadow: 0 8px 26px rgba(15, 23, 42, 0.08);
        display: grid;
        grid-template-columns: 235px 1fr;
        overflow: hidden;
    }

    .educator-sidebar {
        background: #fff;
        border-right: 1px solid #e5e7eb;
        padding: 28px 22px 0;
        display: flex;
        flex-direction: column;
        overflow: hidden;
    }

    .educator-logo {
        margin: 0 0 34px;
        color: #6243db;
        font-size: 2.7rem;
        font-weight: 900;
        text-align: center;
    }

    .educator-menu {
        display: flex;
        flex-direction: column;
        gap: 12px;
        z-index: 5;
    }

    .educator-link {
        display: flex;
        align-items: center;
        gap: 14px;
        padding: 14px 18px;
        border-radius: 12px;
        color: #111827;
        text-decoration: none;
        font-size: 1rem;
        font-weight: 700;
    }

    .educator-link span {
        width: 24px;
        color: #334155;
        text-align: center;
        font-size: 1.25rem;
    }

    .educator-link.active {
        color: #6243db;
        background: #ece9ff;
    }

    .educator-link.active span { color: #6243db; }

    .sidebar-decoration {
        margin-top: auto;
        position: relative;
        width: calc(100% + 44px);
        margin-left: -22px;
        height: 230px;
        overflow: hidden;
    }

    .cloud {
        position: absolute;
        background: #dbeafe;
        border-radius: 50px;
        z-index: 1;
    }

    .cloud::before, .cloud::after {
        content: "";
        position: absolute;
        background: #dbeafe;
        border-radius: 50%;
    }

    .cloud-left { width: 56px; height: 18px; left: 48px; top: 22px; }
    .cloud-left::before { width: 20px; height: 20px; left: 8px; top: -10px; }
    .cloud-left::after { width: 24px; height: 24px; left: 26px; top: -12px; }
    .cloud-right { width: 62px; height: 20px; right: 48px; top: 8px; }
    .cloud-right::before { width: 22px; height: 22px; left: 8px; top: -10px; }
    .cloud-right::after { width: 26px; height: 26px; left: 28px; top: -13px; }

    .ground-illustration {
        position: absolute;
        left: 0;
        right: 0;
        bottom: 0;
        height: 150px;
        background: #98c943;
        border-top-left-radius: 42px;
        border-top-right-radius: 42px;
        z-index: 2;
    }

    .house-illustration {
        position: absolute;
        left: 50%;
        bottom: 26px;
        transform: translateX(-50%);
        width: 132px;
        height: 105px;
        z-index: 4;
    }

    .house-roof {
        position: absolute;
        left: 50%;
        top: 0;
        transform: translateX(-50%);
        width: 112px;
        height: 42px;
        background: #6243db;
        clip-path: polygon(50% 0%, 100% 100%, 0% 100%);
        z-index: 5;
    }

    .house-body {
        position: absolute;
        left: 50%;
        bottom: 0;
        transform: translateX(-50%);
        width: 104px;
        height: 62px;
        background: #ffd166;
        border-radius: 6px;
        display: flex;
        align-items: flex-end;
        justify-content: center;
        gap: 12px;
        padding-bottom: 12px;
        z-index: 4;
    }

    .house-window { width: 16px; height: 24px; background: #5fc0ef; border-radius: 4px; margin-bottom: 2px; }
    .house-door { width: 20px; height: 34px; background: #6243db; border-radius: 8px 8px 0 0; }
    .tree { position: absolute; bottom: 16px; z-index: 3; }
    .tree-left { left: 42px; }
    .tree-right { right: 42px; }
    .tree-top { width: 30px; height: 64px; background: #8ac53a; border-radius: 20px; margin: 0 auto; }
    .tree-trunk { width: 4px; height: 62px; background: #9c5c2f; margin: -2px auto 0; border-radius: 3px; }

    .educator-main {
        padding: 34px 48px 0;
        display: flex;
        flex-direction: column;
        min-width: 0;
    }

    .page-top {
        display: flex;
        align-items: center;
        justify-content: space-between;
        margin-bottom: 26px;
    }

    .title-row {
        display: flex;
        align-items: center;
        gap: 24px;
    }

    .back-link {
        color: #6243db;
        text-decoration: none;
        font-size: 2.2rem;
        line-height: 1;
    }

    .title-row h1 {
        margin: 0;
        font-size: 2.05rem;
        font-weight: 900;
        color: #111827;
    }

    .qr-corner {
        color: #6243db;
        font-size: 2rem;
        font-weight: 900;
    }

    .intro-card, .records-card, .stat-card {
        background: #fff;
        border-radius: 16px;
        box-shadow: 0 4px 18px rgba(15, 23, 42, 0.07);
    }

    .intro-card {
        display: flex;
        align-items: center;
        gap: 20px;
        padding: 22px 26px;
        margin-bottom: 20px;
    }

    .intro-icon {
        width: 62px;
        height: 62px;
        border-radius: 16px;
        background: #ece9ff;
        color: #6243db;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 2rem;
        font-weight: 900;
        flex-shrink: 0;
    }

    .intro-card h2 { margin: 0 0 6px; font-size: 1.35rem; font-weight: 900; }
    .intro-card p { margin: 0; color: #64748b; }

    /* === QR generisanje === */
    .qr-flash {
        background: #ece9ff;
        color: #6243db;
        font-weight: 800;
        padding: 14px 18px;
        border-radius: 14px;
        margin-bottom: 18px;
    }

    .qr-generate-card {
        background: #fff;
        border-radius: 16px;
        box-shadow: 0 4px 18px rgba(15, 23, 42, 0.07);
        padding: 24px 26px;
        margin-bottom: 20px;
        display: grid;
        grid-template-columns: 1fr 230px;
        gap: 26px;
        align-items: center;
    }

    .qr-generate-info h2 { margin: 0 0 8px; font-size: 1.3rem; font-weight: 900; }
    .qr-generate-info p { margin: 0 0 18px; color: #64748b; }

    .qr-type-toggle {
        display: flex;
        gap: 12px;
        margin-bottom: 16px;
    }

    .qr-type-btn {
        border: 1px solid #d8def0;
        border-radius: 999px;
        padding: 10px 20px;
        font-weight: 800;
        color: #334155;
        background: #fff;
        cursor: pointer;
        font-size: 1rem;
    }

    .qr-type-btn.active {
        background: #ece9ff;
        color: #6243db;
        border-color: #c8bdf6;
    }

    .qr-generate-btn {
        height: 54px;
        padding: 0 26px;
        border: none;
        border-radius: 12px;
        background: #6243db;
        color: #fff;
        font-weight: 900;
        font-size: 1rem;
        cursor: pointer;
    }

    .qr-generate-btn:hover { background: #5436c4; }

    .qr-valid-text { margin: 16px 0 0; color: #475569; font-size: 0.92rem; }

    .qr-generate-preview {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
    }

    .qr-box {
        width: 190px;
        height: 190px;
        border: 1px solid #e5e7eb;
        border-radius: 14px;
        background: #fff;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 8px;
        box-sizing: border-box;
    }

    .qr-box img, .qr-box canvas { display: block; }

    .qr-box-empty { color: #c4c9d6; font-size: 4rem; }

    .qr-type-label { margin: 12px 0 0; font-weight: 800; color: #6243db; }
    .qr-empty-label { margin: 12px 0 0; color: #94a3b8; font-size: 0.9rem; text-align: center; }

    @@media (max-width: 700px) {
        .qr-generate-card { grid-template-columns: 1fr; }
        .stats-grid { grid-template-columns: 1fr; }
    }

    .attendance-filter {
        display: grid;
        grid-template-columns: 1fr 1fr 1fr 120px;
        gap: 16px;
        align-items: end;
        margin-bottom: 20px;
    }

    .field-block label {
        display: block;
        margin-bottom: 8px;
        color: #111827;
        font-weight: 900;
    }

    .field-block select,
    .date-box {
        width: 100%;
        height: 54px;
        border: 1px solid #d8def0;
        border-radius: 12px;
        background: #fff;
        padding: 0 14px;
        font-size: 1rem;
        color: #111827;
        box-sizing: border-box;
    }

    .date-box {
        position: relative;
        display: flex;
        align-items: center;
        justify-content: space-between;
    }

    .date-box input[type="date"] {
        position: absolute;
        inset: 0;
        opacity: 0;
        cursor: pointer;
    }

    .calendar-symbol { color: #6243db; font-weight: 900; }

    .attendance-filter button {
        height: 54px;
        border: none;
        border-radius: 12px;
        background: #6243db;
        color: white;
        font-weight: 900;
        font-size: 1rem;
        cursor: pointer;
    }

    .stats-grid {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 18px;
        margin-bottom: 20px;
    }

    .stat-card {
        padding: 18px 22px;
        min-height: 130px;
    }

    .stat-icon {
        width: 42px;
        height: 42px;
        border-radius: 50%;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        margin-bottom: 12px;
        font-weight: 900;
    }

    .stat-icon.green { color: #15803d; background: #dcfce7; }
    .stat-icon.purple { color: #6243db; background: #ece9ff; }
    .stat-icon.red { color: #dc2626; background: #fee2e2; }

    .stat-card h3 { margin: 0; font-size: 1rem; font-weight: 900; color: #111827; }
    .stat-card strong { display: block; margin-top: 14px; font-size: 2.4rem; color: #111827; }

    .records-card {
        padding: 24px;
        margin-bottom: 22px;
    }

    .records-heading {
        display: flex;
        align-items: center;
        justify-content: space-between;
        margin-bottom: 18px;
    }

    .records-heading h2 { margin: 0; font-size: 1.35rem; font-weight: 900; }
    .records-heading span { color: #6243db; font-weight: 900; background: #ece9ff; padding: 8px 14px; border-radius: 999px; }

    .empty-state {
        text-align: center;
        padding: 36px 20px;
        color: #64748b;
    }

    .empty-state div { color: #6243db; font-size: 3rem; margin-bottom: 12px; }
    .empty-state h3 { margin: 0 0 8px; color: #111827; }
    .empty-state p { margin: 0; }

    .records-list {
        display: flex;
        flex-direction: column;
        gap: 14px;
    }

    .record-item {
        display: grid;
        grid-template-columns: 58px 1fr 120px 120px;
        gap: 16px;
        align-items: center;
        border: 1px solid #e5e7eb;
        border-radius: 14px;
        padding: 16px;
        background: #fff;
    }

    .record-item.rejected {
        border-color: #fecaca;
        background: #fff7f7;
    }

    .record-icon {
        width: 48px;
        height: 48px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.5rem;
        font-weight: 900;
    }

    .record-icon.arrival { color: #15803d; background: #dcfce7; }
    .record-icon.departure { color: #6243db; background: #ece9ff; }

    .record-main h3 { margin: 0 0 4px; font-size: 1rem; font-weight: 900; }
    .record-main p { margin: 0; color: #64748b; font-size: .94rem; }

    .record-meta { text-align: right; }
    .record-meta strong { display: block; font-size: .9rem; color: #111827; }
    .record-meta span { color: #64748b; }

    .record-status {
        text-align: center;
        border-radius: 999px;
        padding: 8px 10px;
        font-weight: 900;
        font-size: .9rem;
        text-transform: capitalize;
    }

    .record-status.good { background: #dcfce7; color: #15803d; }
    .record-status.bad { background: #fee2e2; color: #dc2626; }

.educator-bottom-nav {
    grid-column: 2;
    width: 100%;
    min-height: 74px;
    margin-top: auto;
    border-top: 1px solid #e5e7eb;
    background: #ffffff;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 46px;
    align-self: end;
}

.bottom-link {
    display: flex;
    align-items: center;
    gap: 9px;
    text-decoration: none;
    color: #475569;
    font-weight: 800;
    font-size: 0.95rem;
    white-space: nowrap;
}

.bottom-link:hover {
    color: #7447d8;
    text-decoration: none;
}

.bottom-link.active {
    color: #7447d8;
}

.bottom-link span {
    font-size: 1.2rem;
    line-height: 1;
}

    @@media (max-width: 800px) {
        .educator-attendance-card { grid-template-columns: 1fr; }
        .educator-sidebar { display: none; }
        .bottom-nav { gap: 20px; flex-wrap: wrap; }
    }


</style>


<style>
    /* === ODGAJATELJ UI STANDARDIZACIJA === */
    body {
        background: #eef3ff !important;
    }

    .educator-page,
    .educator-attendance-page,
    .educator-summary-page {
        min-height: 100vh;
        padding: 22px 0;
        font-family: Arial, sans-serif;
        color: #071733;
    }

    .educator-card,
    .educator-attendance-card,
    .educator-summary-card {
        width: 100%;
        max-width: 1250px;
        min-height: 760px;
        margin: 0 auto;
        background: #fbfaff;
        border-radius: 22px;
        box-shadow: 0 8px 26px rgba(15, 23, 42, 0.08);
        display: grid;
        grid-template-columns: 235px 1fr;
        overflow: hidden;
    }

    .educator-sidebar {
        background: #ffffff;
        border-right: 1px solid #e5e7eb;
        padding: 28px 22px 0;
        display: flex;
        flex-direction: column;
        overflow: hidden;
        min-width: 0;
    }

    .educator-logo {
        margin: 0 0 34px;
        color: #6d42db;
        font-size: 2.7rem;
        line-height: 1;
        font-weight: 900;
        text-align: center;
    }

    .educator-menu {
        position: relative;
        z-index: 10;
        display: flex;
        flex-direction: column;
        gap: 10px;
    }

    .educator-link {
        min-height: 54px;
        display: flex;
        align-items: center;
        gap: 14px;
        padding: 0 18px;
        border-radius: 12px;
        color: #071733;
        text-decoration: none;
        font-size: 1rem;
        font-weight: 800;
        box-sizing: border-box;
    }

    .educator-link:hover {
        color: #6d42db;
        background: #f3efff;
        text-decoration: none;
    }

    .educator-link span {
        width: 24px;
        text-align: center;
        font-size: 1.25rem;
        line-height: 1;
        flex-shrink: 0;
    }

    .educator-link.active {
        color: #7447d8;
        background: #ece6ff;
    }

    .badge-icon { position: relative; }
    .badge-icon b,
    .bell-badge {
        position: absolute;
        top: -9px;
        right: -11px;
        min-width: 18px;
        height: 18px;
        padding: 0 5px;
        border-radius: 999px;
        background: #ef4444;
        color: white;
        font-size: 0.68rem;
        font-weight: 900;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        line-height: 1;
    }

  

  

    .educator-side-art {
        margin-top: auto;
        position: relative;
        height: 210px;
        flex-shrink: 0;
        overflow: hidden;
        z-index: 1;
        pointer-events: none;
    }

    .side-cloud {
        position: absolute;
        background: #dbeafe;
        border-radius: 999px;
    }

    .side-cloud::before,
    .side-cloud::after {
        content: "";
        position: absolute;
        background: #dbeafe;
        border-radius: 50%;
    }

    .side-cloud-1 {
        width: 56px;
        height: 18px;
        left: 42px;
        top: 18px;
    }

    .side-cloud-1::before { width: 22px; height: 22px; left: 8px; top: -10px; }
    .side-cloud-1::after { width: 26px; height: 26px; left: 26px; top: -13px; }

    .side-cloud-2 {
        width: 64px;
        height: 20px;
        right: 38px;
        top: 4px;
    }

    .side-cloud-2::before { width: 24px; height: 24px; left: 9px; top: -11px; }
    .side-cloud-2::after { width: 28px; height: 28px; left: 30px; top: -14px; }

    .side-kindergarten {
        position: absolute;
        left: 50%;
        bottom: 28px;
        width: 120px;
        height: 105px;
        transform: translateX(-50%);
        z-index: 4;
    }

    .side-roof {
        position: absolute;
        left: 50%;
        top: 0;
        width: 104px;
        height: 44px;
        transform: translateX(-50%);
        background: #6d42db;
        clip-path: polygon(50% 0%, 100% 100%, 0% 100%);
        z-index: 5;
    }

    .side-building {
        position: absolute;
        left: 50%;
        bottom: 0;
        width: 104px;
        height: 62px;
        transform: translateX(-50%);
        background: #ffd35c;
        border-radius: 7px;
        display: flex;
        align-items: flex-end;
        justify-content: center;
        gap: 12px;
        padding-bottom: 12px;
        box-sizing: border-box;
        z-index: 4;
    }

    .side-window {
        width: 16px;
        height: 24px;
        background: #55bce8;
        border-radius: 5px;
    }

    .side-door {
        width: 22px;
        height: 34px;
        background: #2bb3a3;
        border-radius: 10px 10px 0 0;
    }

    .side-tree {
        position: absolute;
        bottom: 42px;
        width: 38px;
        height: 84px;
        background: #7ed832;
        border-radius: 22px;
        z-index: 3;
    }

    .side-tree::after {
        content: "";
        position: absolute;
        left: 50%;
        bottom: -42px;
        width: 4px;
        height: 58px;
        transform: translateX(-50%);
        background: #9c5c2f;
        border-radius: 3px;
    }

    .side-tree-left { left: 32px; }
    .side-tree-right { right: 32px; }

    .educator-main,
    .attendance-main,
    .summary-main {
        grid-column: 2;
        padding: 34px 48px 0;
        min-width: 0;
        display: flex;
        flex-direction: column;
    }

    .educator-main-scroll {
        overflow-y: auto;
    }

    .educator-bottom-nav {
        grid-column: 2;
        margin: auto -48px 0;
        width: calc(100% + 96px);
        min-height: 74px;
        border-top: 1px solid #e5e7eb;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 40px;
        padding: 14px 24px;
        box-sizing: border-box;
        background: #ffffff;
        flex-wrap: wrap;
        align-self: end;
    }

    .bottom-link {
        display: flex;
        align-items: center;
        gap: 9px;
        text-decoration: none;
        color: #475569;
        font-weight: 800;
        font-size: 0.98rem;
    }

    .bottom-link:hover {
        color: #7447d8;
        text-decoration: none;
    }

    .bottom-link.active {
        color: #7447d8;
    }

    .bottom-link span {
        position: relative;
        font-size: 1.25rem;
        line-height: 1;
    }

    @@media (max-width: 1000px) {
        .educator-card,
        .educator-attendance-card,
        .educator-summary-card {
            grid-template-columns: 1fr;
            border-radius: 0;
        }

        .educator-sidebar {
            display: none;
        }

        .educator-main,
        .attendance-main,
        .summary-main,
        .educator-bottom-nav {
            grid-column: 1;
        }
    }
</style>


@section Scripts {
    <script src="https://cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js"></script>
    <script>
        (function () {
            var tipInput = document.getElementById('odabraniTipInput');
            var buttons = document.querySelectorAll('.qr-type-btn');
            var qrBox = document.getElementById('qrcode');
            var typeLabel = document.getElementById('qrTypeLabel');

            function trenutniTip() { return tipInput ? tipInput.value : 'DOLAZAK'; }

            function renderQR() {
                if (!qrBox || typeof QRCode === 'undefined') return;
                var token = qrBox.getAttribute('data-token');
                if (!token) return;

                var payload = 'EVRTIC|' + token + '|' + trenutniTip();
                qrBox.innerHTML = '';
                new QRCode(qrBox, {
                    text: payload,
                    width: 174,
                    height: 174,
                    correctLevel: QRCode.CorrectLevel.M
                });

                if (typeLabel) {
                    typeLabel.textContent = (trenutniTip() === 'DOLAZAK') ? 'Tip: Dolazak' : 'Tip: Odlazak';
                }
            }

            buttons.forEach(function (btn) {
                btn.addEventListener('click', function () {
                    buttons.forEach(function (b) { b.classList.remove('active'); });
                    btn.classList.add('active');
                    if (tipInput) tipInput.value = btn.getAttribute('data-tip');
                    renderQR();
                });
            });

            renderQR();
        })();
    </script>
}
